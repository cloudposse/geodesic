AWS_DEFAULT_PROFILE=cloudposse-ops                  # Profile to use in ~/.aws/config
CLUSTER_DNS_ZONE=dev.cloudposse.com                 # Parent zone for cluster (e.g. example.com)
CLUSTER_PREFIX=demo                                 # Short name of cluster (e.g. demo)
CLUSTER_STATE_BUCKET_REGION=us-east-1               # Primary region of bucket
GITHUB_USERNAME=admin                               # Username to use for connecting to cluster
