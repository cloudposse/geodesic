# Usage: use_envrc
#
# Loads all files whose names end with `.envrc` (but not the actual `.envrc`) using `source_env`
# Note that `source_env` does not perform any security checks
#
function use_envrc() {
	for file in *.envrc; do
	  source_env $file
	done
}
