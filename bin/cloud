#!/bin/bash
cd "$GEODESIC_PATH/modules"

set -o pipefail

while true; do
  arg=$1
  if [ -d "$arg" ]; then
    set -- "${@:2}"
    cd "$arg"
  else
    make --no-print-directory $*
    exit $?
  fi
done
