server:
  ingress:
    annotations:
      kubernetes.io/tls-acme: "true"
      external-dns.alpha.kubernetes.io/ttl: "60"
gateway:
  ingress:
    annotations:
      kubernetes.io/tls-acme: "true"
      external-dns.alpha.kubernetes.io/ttl: "60"
